import type {Config} from "./types";

export const config: Config = {
    token: '<github-token>',
    projects: [
        {
            name: 'Video',
            owner: 'Creatorsgarten',
            repo: 'videos'
        },
    ]
}
